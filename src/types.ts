
export interface FormValues {
  email: string;
  password: string;
  name? : string;
  subject? : string,
  message? : string,
  
}
export interface ContactUsValues {
  email: string;
  subject? : string,
  message? : string,
  
}